# CROCS Teaching v1 Sandbox — SIM-UI-4 + G1 presentation candidate

This static review surface preserves the cumulative SIM-UI-1/2/3/4 shell,
including P2/P3 drafts, P4/P5 views and P6/six-year anchors. G1 adds a bounded
desktop presentation candidate over that existing authority.

## Cumulative authority

1. `DCS-SIM-VIS-001_v1.0`
2. `CROCS_SANDBOX_SIM_UI1_CODEX_IMPLEMENTATION_CONTRACT_V1_0`
3. `DCS-SIM-VIS-001-A01_D6_Reduction_Portfolio_v1.0`
4. `CROCS_SANDBOX_SIM_UI2_CODEX_IMPLEMENTATION_CONTRACT_V1_0`
5. `CROCS_SANDBOX_SIM_UI3_CODEX_IMPLEMENTATION_CONTRACT_v1.0` / A02
6. `CROCS_SANDBOX_SIM_UI4_CODEX_IMPLEMENTATION_CONTRACT_v1.0` / A03
7. `CROCS_TEACHING_G1_BROWSER_IMPLEMENTATION_CONTRACT_V1_2` / DCS-005 + DCS-006 + DCS-010

The browser consumes cumulative `simUi2`, `simUi3` and `simUi4` metadata produced
by the governed projection model. P2 D3/D4 and P3 D5/D6/D7/D10 controls validate only canonical catalog
options and create non-authoritative UI drafts. D6 legacy options remain
metadata and are never active or mapped. Actual Reduction remains read-only
Carbon Core output. Missing rules/parameters remain blocked with null values.

## Scene artifacts

The six approved scene identities and filenames are retained, but their pixel
bytes are unavailable. Each therefore remains a context-only
`MISSING_ARTIFACT` with `assetPath: null`; no substitute artwork was inferred.

## Provenance

`dataBaselineHead` identifies governed projection data. Ordered
`authorityReferences` preserve base and amendment authority.
`implementationHead` is null in checked-in source and is populated with the
exact Git SHA only during deterministic export.

Serve this directory with `python3 -m http.server 8000` and open
`http://localhost:8000/`.

## Interaction integrity

Locked P2/P3 workbenches are strictly read-only. Saved UI drafts and selections
rehydrate without generating a new reference. All selects require explicit
learner choice and have no first-option default. The seven P2 homes, including
all missing/null authority states, are rendered from the governed projection.
Current projection schema is `CROCS_TEACHING_V1_SIM_UI_4_STATIC_PROJECTION_V1`;
all earlier authority remains preserved. G1 presentation authority is declared
separately and does not revise the data schema.

## G1 presentation implementation candidate

Authority: G1-BROWSER-001 v1.2 / DCS-005 + DCS-006 + DCS-010 / Design Master v1.12. P6 classroom layout approval was persisted at 4d5123d59068695619dbf99f0d6a148d2953b2e3 before implementation. Original implementation approval was persisted at 466ff44abddc928d29d0a62e57ad0630b5d96ee8; the two-clause reconciliation was approved and persisted at eb795ae4207ea7b280b47c7196b968168d87ada5 before related copy/metadata/Figma updates. Cumulative SIM-UI-3/A02 and SIM-UI-4/A03 remain the business/browser authority.

The G1 scene, campus navigation, five fictional role portraits, read-only fact/event/reflection views and progressive detail use the existing browser state. The standalone G1 asset collection is governed by g1-assets.json, with source/generation hashes and Figma nodes. Original six SIM-UI assets remain MISSING_ARTIFACT; they have not been replaced or re-identified.

G1-R01 and G1-R02 are RESOLVED-DESIGN under DCS-006: retain A01's five active D6 classes and retain D8/D9 in BUSINESS_DECISION. The earlier conflicting approved records and four historical pictograms remain preserved; no legacy mapping or ownership migration is introduced. Factory D8/D9 affordances display existing read-only identity/status only. Other unresolved parameters and actual browser/classroom acceptance remain open.

No domain model, parameter, draft authority, annual gate or production projection data changed. This is not a deployment or classroom acceptance. Desktop is primary; mobile evidence remains historical and is not a completion gate.
